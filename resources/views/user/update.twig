{% extends 'templates/app.twig' %}

{% block title %}Edit Profile{% endblock %}

{% block extrajs %}
	<script>
	// shamelessly stolen from stackoverflow
	$(function() {
		$("textarea").bind('input propertychange', function() {
			var maxLength  = 500;
			var usedLength = $(this).val().length;
			if (usedLength > maxLength) {
				$(this).val($(this).val().substring(0, maxLength));
				usedLength = $(this).val().length;
			}
			$('#remaining').html(maxLength - usedLength + ' characters remaining');
		});
	});
	</script>
{% endblock %}

{% block extracss %}
	<style>
	#bio {
		resize: vertical;
	}
	</style>
{% endblock %}

{% block content %}
	<div class="row">
		<div class="col-md-6 col-md-offset-3">
			<div class="panel panel-default">
				<div class="panel-heading">
					<h3 class="panel-title">Edit Profile</h3>
				</div>

				<div class="panel-body">
					<p>
						Need to change your password? Click <a href="{{ path_for('auth.password.change') }}">here</a>!
					</p>

					<form action="{{ path_for('user.profile.edit') }}" method="post" autocomplete="off">
						<h4>
							Details
						</h4>

						<div class="form-group{{ errors.website ? ' has-error' : '' }}">
							<label for="website">Website</label>
							<input type="url" name="website" id="website" placeholder="http://domain.ext" class="form-control" value="{{ old.website | default(auth.user.website) }}">
							{% if errors.website %}
								<span class="help-block">{{ errors.website | first }}</span>
							{% endif %}
						</div>

						<div class="form-group{{ errors.bio ? ' has-error' : '' }}">
							<label for="bio">Bio (supports Github-Flavored Markdown)</label>
							<textarea name="bio" id="bio" cols="30" rows="10" class="form-control" placeholder="Your life story.">{{ old.bio | default(auth.user.bio) }}</textarea>
							<span class="help-block" id="remaining"></span>
							{% if errors.bio %}
								<span class="help-block">{{ errors.bio | first }}</span>
							{% endif %}
						</div>

						<h4>
							Default privacy setting
						</h4>

						<div class="radio">
							<label>
								<input type="radio" name="privacy" id="public" value="public"{% if auth.user.default_privacy_state == 0 %} checked{% endif %}>
								Public
							</label>
						</div>

						<div class="radio">
							<label>
								<input type="radio" name="privacy" id="unlisted" value="unlisted"{% if auth.user.default_privacy_state == 1 %} checked{% endif %}>
								Unlisted (not displayed on profile)
							</label>
						</div>

						<div class="radio">
							<label>
								<input type="radio" name="privacy" id="private" value="private"{% if auth.user.default_privacy_state == 2 %} checked{% endif %}>
								Private (requires login to view)
							</label>
						</div>

						<button type="submit" class="btn btn-default">Submit</button>

						{{ csrf.field | raw }}
					</form>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
